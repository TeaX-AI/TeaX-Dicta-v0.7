name: stage-d

on:
  workflow_dispatch:

concurrency:
  group: stage-d
  cancel-in-progress: false

jobs:
  train:
    runs-on: macos-latest
    timeout-minutes: 350
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install deps
        run: |
          cd DictionariDev
          pip install --upgrade pip
          pip install --no-cache-dir torch
          pip install --no-cache-dir -r requirements.txt

      - name: Download previous checkpoint
        id: ckpt
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          mkdir -p inputs/prev
          if gh release view ckpt-stage-d > /dev/null 2>&1; then
            TARGET_RELEASE="ckpt-stage-d"
          elif gh release view ckpt-stage-c > /dev/null 2>&1; then
            TARGET_RELEASE="ckpt-stage-c"
          else
            TARGET_RELEASE=""
          fi
          if [ -n "$TARGET_RELEASE" ]; then
            for i in 1 2 3 4 5; do
              echo "[download] attempt $i/5 from $TARGET_RELEASE"
              if gh release download "$TARGET_RELEASE" \
                --pattern "model.pt" \
                --pattern "meta.json" \
                --pattern "dictionari-model.json" \
                --dir inputs/prev \
                --clobber; then
                echo "[download] success"
                break
              fi
              if [ "$i" -eq 5 ]; then
                echo "[download] all 5 attempts failed, aborting job"
                exit 1
              fi
              echo "[download] failed, waiting 15s before retry"
              sleep 15
            done
          else
            echo "[download] no release found, first run"
          fi
          if [ -f inputs/prev/model.pt ]; then
            echo "found=true" >> $GITHUB_OUTPUT
          else
            echo "found=false" >> $GITHUB_OUTPUT
          fi

      - name: Train stage-d
        run: |
          cd DictionariDev
          EXTRA=""
          if [ "${{ steps.ckpt.outputs.found }}" = "true" ]; then
            EXTRA="--resume_from ../inputs/prev/model.pt"
          fi
          python Train.py \
            --stage stage_d \
            --dataset microsoft/NextCoderDataset \
            --output_dir ../outputs/stage_d \
            --d_model 640 \
            --level_sizes "7,14,98" \
            --hidden_mult 3 \
            --top_k 2 \
            --epochs 2 \
            --max_minutes 320 \
            $EXTRA

      - name: Upload checkpoint
        if: always()
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          DIR=./outputs/stage_d
          if [ ! -f "$DIR/model.pt" ]; then
            echo "no checkpoint produced"
            exit 0
          fi
          gh release view ckpt-stage-d > /dev/null 2>&1 || \
            gh release create ckpt-stage-d \
              --title "stage-d checkpoint" \
              --notes "auto checkpoint" \
              --target ${{ github.ref_name }}
          gh release upload ckpt-stage-d "$DIR/model.pt" --clobber
          gh release upload ckpt-stage-d "$DIR/meta.json" --clobber
          gh release upload ckpt-stage-d "$DIR/dictionari-model.json" --clobber

      - name: Trigger next
        if: success()
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          DIR=./outputs/stage_d
          DONE="false"
          if [ -f "$DIR/meta.json" ]; then
            DONE=$(python -c "import json; print(str(json.load(open('$DIR/meta.json')).get('complete', False)).lower())")
          fi
          if [ "$DONE" = "true" ]; then
            echo "stage-d complete, triggering stage-e"
            gh workflow run stage-e.yml --ref ${{ github.ref_name }}
          else
            echo "stage-d incomplete, re-triggering stage-d"
            gh workflow run stage-d.yml --ref ${{ github.ref_name }}
          fi