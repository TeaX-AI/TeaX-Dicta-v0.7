name: stage-d

on:
  workflow_dispatch:

concurrency:
  group: stage-d
  cancel-in-progress: false

jobs:
  train:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install deps
        run: |
          pip install --upgrade pip
          pip install --no-cache-dir torch --index-url https://download.pytorch.org/whl/cpu
          pip install --no-cache-dir -r requirements.txt

      - name: Download previous checkpoint
        id: ckpt
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          mkdir -p inputs/prev
          if gh release view ckpt-stage-d > /dev/null 2>&1; then
            gh release download ckpt-stage-d \
              --pattern "model.pt" \
              --pattern "meta.json" \
              --dir inputs/prev \
              --clobber
          elif gh release view ckpt-stage-c > /dev/null 2>&1; then
            gh release download ckpt-stage-c \
              --pattern "model.pt" \
              --pattern "meta.json" \
              --dir inputs/prev \
              --clobber
          fi
          if [ -f inputs/prev/model.pt ]; then
            echo "found=true" >> $GITHUB_OUTPUT
          else
            echo "found=false" >> $GITHUB_OUTPUT
          fi

      - name: Train stage-d
        run: |
          EXTRA=""
          if [ "${{ steps.ckpt.outputs.found }}" = "true" ]; then
            EXTRA="--resume_from ./inputs/prev/model.pt"
          fi
          python train.py \
            --stage stage_d \
            --dataset teknium/OpenHermes-2.5 \
            --output_dir ./outputs/teax-dicta-v0.7/stage_d \
            --epochs 2 \
            --max_minutes 320 \
            --max_samples 0 \
            $EXTRA

      - name: Upload checkpoint
        if: always()
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          DIR=./outputs/teax-dicta-v0.7/stage_d
          if [ ! -f "$DIR/model.pt" ]; then
            echo "no checkpoint produced"
            exit 0
          fi
          gh release view ckpt-stage-d > /dev/null 2>&1 || \
            gh release create ckpt-stage-d \
              --title "stage-d checkpoint" \
              --notes "auto checkpoint" \
              --target main
          gh release upload ckpt-stage-d "$DIR/model.pt" --clobber
          gh release upload ckpt-stage-d "$DIR/meta.json" --clobber

      - name: Trigger next
        if: success()
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          DIR=./outputs/teax-dicta-v0.7/stage_d
          DONE="false"
          if [ -f "$DIR/meta.json" ]; then
            DONE=$(python -c "import json; print(str(json.load(open('$DIR/meta.json')).get('complete', False)).lower())")
          fi
          if [ "$DONE" = "true" ]; then
            echo "stage-d complete, triggering trans-gguf"
            gh workflow run trans-gguf.yml --ref main
          else
            echo "stage-d incomplete, re-triggering stage-d"
            gh workflow run stage-d.yml --ref main
          fi