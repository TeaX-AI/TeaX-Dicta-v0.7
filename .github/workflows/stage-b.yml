name: stage-b

on:
  workflow_dispatch:

concurrency:
  group: stage-b
  cancel-in-progress: false

jobs:
  train:
    runs-on: macos-latest
    timeout-minutes: 350
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install deps
        run: |
          cd DictionariDev
          pip install --upgrade pip
          pip install --no-cache-dir torch
          pip install --no-cache-dir -r requirements.txt

      - name: Download previous checkpoint
        id: ckpt
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          mkdir -p inputs/prev
          if gh release view ckpt-stage-b > /dev/null 2>&1; then
            TARGET_RELEASE="ckpt-stage-b"
          elif gh release view ckpt-stage-a > /dev/null 2>&1; then
            TARGET_RELEASE="ckpt-stage-a"
          else
            TARGET_RELEASE=""
          fi
          if [ -n "$TARGET_RELEASE" ]; then
            for i in 1 2 3 4 5; do
              echo "[download] attempt $i/5 from $TARGET_RELEASE"
              if gh release download "$TARGET_RELEASE" \
                --pattern "model.pt" \
                --pattern "meta.json" \
                --pattern "dictionari-model.json" \
                --dir inputs/prev \
                --clobber; then
                echo "[download] success"
                break
              fi
              if [ "$i" -eq 5 ]; then
                echo "[download] all 5 attempts failed, aborting job"
                exit 1
              fi
              echo "[download] failed, waiting 15s before retry"
              sleep 15
            done
          else
            echo "[download] no release found, first run"
          fi
          if [ -f inputs/prev/model.pt ]; then
            echo "found=true" >> $GITHUB_OUTPUT
          else
            echo "found=false" >> $GITHUB_OUTPUT
          fi

      - name: Train stage-b
        run: |
          cd DictionariDev
          EXTRA=""
          if [ "${{ steps.ckpt.outputs.found }}" = "true" ]; then
            EXTRA="--resume_from ../inputs/prev/model.pt"
          fi
          python Train.py \
            --stage stage_b \
            --dataset anon8231489123/ShareGPT_Vicuna_unfiltered \
            --output_dir ../outputs/stage_b \
            --d_model 640 \
            --layer_experts "12,24,48" \
            --hidden_mult 3 \
            --top_k 2 \
            --epochs 2 \
            --max_minutes 320 \
            $EXTRA

      - name: Upload checkpoint
        if: always()
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          DIR=./outputs/stage_b
          if [ ! -f "$DIR/model.pt" ]; then
            echo "no checkpoint produced"
            exit 0
          fi
          gh release view ckpt-stage-b > /dev/null 2>&1 || \
            gh release create ckpt-stage-b \
              --title "stage-b checkpoint" \
              --notes "auto checkpoint" \
              --target ${{ github.ref_name }}
          gh release upload ckpt-stage-b "$DIR/model.pt" --clobber
          gh release upload ckpt-stage-b "$DIR/meta.json" --clobber
          gh release upload ckpt-stage-b "$DIR/dictionari-model.json" --clobber

      - name: Trigger next
        if: success()
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          DIR=./outputs/stage_b
          DONE="false"
          if [ -f "$DIR/meta.json" ]; then
            DONE=$(python -c "import json; print(str(json.load(open('$DIR/meta.json')).get('complete', False)).lower())")
          fi
          if [ "$DONE" = "true" ]; then
            echo "stage-b complete, triggering stage-c"
            gh workflow run stage-c.yml --ref ${{ github.ref_name }}
          else
            echo "stage-b incomplete, re-triggering stage-b"
            gh workflow run stage-b.yml --ref ${{ github.ref_name }}
          fi